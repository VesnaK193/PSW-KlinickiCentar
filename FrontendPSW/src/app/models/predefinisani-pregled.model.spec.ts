import { PredefinisaniPregled } from './predefinisani-pregled.model';

describe('PredefinisaniPregled', () => {
  it('should create an instance', () => {
    expect(new PredefinisaniPregled()).toBeTruthy();
  });
});
